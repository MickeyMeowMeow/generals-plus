name: "Setup pnpm & node"
description: "Installs pnpm, node, and dependencies with caching"

runs:
  using: "composite"
  steps:
    - uses: pnpm/action-setup@v5

    - uses: actions/setup-node@v5
      with:
        node-version: 24
        cache: "pnpm"

    - run: pnpm install
      shell: bash
