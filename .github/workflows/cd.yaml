name: CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  sync:
    runs-on: ubuntu-latest
    container:
      image: alpine/git
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - run: git push --force https://oauth2:${{ secrets.GITLAB_TOKEN }}@${{ secrets.GITLAB_URL }} main:main
