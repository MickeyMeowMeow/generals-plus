---
title: 文档概览
description: Generals Plus 的 Fern 文档入口。
---

# Generals Plus API 文档

这里是基于 Fern 的统一文档站点入口。

当前包含两套接口参考：

- `REST 接口`：来自服务端生成的 OpenAPI 规范
- `实时接口`：来自服务端生成的 AsyncAPI 规范

规范源仍然由 `apps/server` 生成，Fern 目录只消费同步后的产物。
